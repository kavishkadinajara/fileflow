# An Empirical Study of on-device inference for privacy

Earlier studies of on-device inference for privacy evaluated single conversions in isolation. H. Weerasinghe and colleagues proposed a similarity-based measure, but it ignores functional elements such as links and formulas. Our protocol differs by scoring every intermediate artifact against both its predecessor and the original.

## Related Work

Earlier studies of layout-aware information extraction evaluated single conversions in isolation. Our protocol differs by scoring every intermediate artifact against both its predecessor and the original. The full context is in the [baseline implementation](https://example.com/baselines/2023).

Our protocol differs by scoring every intermediate artifact against both its predecessor and the original. Benchmark efforts in adjacent areas rely on manual inspection, which does not scale past a few dozen documents. S. Fernando and colleagues proposed a similarity-based measure, but it ignores functional elements such as links and formulas.

We report the following measures:

- Worst-hop location and magnitude
- Run time per conversion
- retention at hop three per chain
- Feature survival ratios for headings, tables, lists, and links

## Discussion

Our synthetic corpus trades naturalism for exact ground truth; we view this as the right trade for measurement studies. A practical implication is path planning: choosing the conversion order can preserve several points of fidelity.

The gap between local and cumulative scores is the clearest signal of silent compounding loss. Failure analysis shows most semantic loss is concentrated in captions and inline emphasis rather than body prose.
